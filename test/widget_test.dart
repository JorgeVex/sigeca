import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder: los tests reales se agregarán por feature', () {
    // Marcador temporal mientras se construye el MVP.
    // Cada feature tendrá sus propios tests más adelante.
    expect(true, isTrue);
  });
}
